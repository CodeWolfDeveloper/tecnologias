# LiveKit — Token e Sala

- `POST /api/meet/token` gera JWT. Cliente acessa `/meet?room=x&name=y` usando `NEXT_PUBLIC_LIVEKIT_URL`.
- Estrutura no banco para rooms/participants/messages/transcripts/recordings/ATA.
